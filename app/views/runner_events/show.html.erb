<h1><%= current_runner.username %>'s page for </h1>

<h3><%= Event.find_by(id: @runner_event.event_id).description %></h3>

<h3><%= event_completed(@runner_event) %></h3>

<p><%= link_to "Home", runner_path(current_runner.id) %></p>
