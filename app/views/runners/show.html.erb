<h1>Runner Show Page</h1>

<h2>Welcome, <%= @runner.username %>!</h2>
Current goal: <%= current_goal_description %>
<%= link_to "Set your goal here", new_runner_goal_path %>
<h4>Just back from a run? Log the details <%= link_to "here!", new_runner_run_path(@runner) %></h4>

<h4>
  Wow, you've gone for <%= @runner.runs.count %> runs!
  Check them out <%= link_to "here", runner_runs_path(@runner.id) %>
</h4>

<h4>
  <%= @runner.username %>, you have
  <%= @runner.goals.count %> goals.
  Check them out!
  <br />
  <% @runner.runner_goals.each do |r_g| %>
    <br />
    <%= r_g.completed %>
  <% end %>
</h4>

<%=
